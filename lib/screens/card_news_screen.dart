import 'package:flutter/material.dart';

class CardNewsScreen extends StatelessWidget {
  const CardNewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("카드뉴스 화면"));
  }
} 